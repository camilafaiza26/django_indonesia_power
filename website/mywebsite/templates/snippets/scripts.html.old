{% load static %}
<script type="text/javascript" src="{%static "vendor/jquery/jquery.js" %}"></script>
<script type="text/javascript" src="{%static "vendor/popper/popper.js" %}"></script>
<script type="text/javascript" src="{%static "vendor/bootstrap/bootstrap.js" %}"></script>
<script type="text/javascript" src="{%static "js/scrolling.js" %}"></script>
<script type="text/javascript" src="{%static "blog/js/scripts.js" %}"></script>
<script type="text/javascript" src="{%static "datepicker/js/bootstrap-datepicker.min.js" %}"></script>
<script src="https://code.jquery.com/jquery-1.12.4.js"></script>
<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

<script type="text/javascript">
 $(function(){
  $("#datepicker1").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker1').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker1").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker1').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker2").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker2').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker3").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker3').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker4").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker4').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker5").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker5').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker6").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker6').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker7").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker7').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker8").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker8').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker9").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker9').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker10").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker10').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker11").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker11').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker12").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker12').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker13").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker13').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker14").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepicker14').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker15").datepicker({
      dateFormat: 'dd/mm/yy',
      onSelect: function(datetext) {

            datetext = datetext;

            $('#datepicker15').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepicker16").datepicker({
      dateFormat: 'dd/mm/yy',
      onSelect: function(datetext) {
datetext = datetext;

            $('#datepicker16').val(datetext);
        }
  });
 });

 $(function(){
  $("#datepickertanggal").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;

            datetext = datetext + " " + h + ":" + m;

            $('#datepickertanggal').val(datetext);
        }
  });
 });
 $(function(){
  $("#datepickerwaktumulai").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;


            $('#datepickerwaktumulai').val(datetext);
        }
  });
 });
 $(function(){
  $("#datepickerwaktuselesai").datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function(datetext) {
            var d = new Date(); // for now

            var h = d.getHours();
            h = (h < 10) ? ("0" + h) : h ;

            var m = d.getMinutes();
            m = (m < 10) ? ("0" + m) : m ;

            var s = d.getSeconds();
            s = (s < 10) ? ("0" + s) : s ;


            $('#datepickerwaktuselesai').val(datetext);
        }
  });
 });

</script>
